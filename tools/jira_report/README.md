# Jira daily report

1. build the docker

    ```
    ./build.sh
    ```

2. edit the config.yaml

3. run the docker

    ```
    ./run.sh daily_report  # 发送日报
    ./run.sh weekly_report # 发送周报
    ```
